CREATE TYPE level AS ENUM (
    'basic',
    'advanced'
);

CREATE TYPE category AS ENUM (
    'technology',
    'aesthetics'
);

CREATE TYPE state AS ENUM (
    'judging',
    'judge_na',
    'judge_ac',
    'reached',
    'unreached'
);

